def getAvailableLetters(lettersGuessed):
  alpha=string.ascii_lowercase
  for char in lettersGuessed:
      if (char in alpha) == True:
          alpha = alpha.replace(char,' _ ')     
  return alpha
